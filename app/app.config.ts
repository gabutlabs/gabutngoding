// app.config.ts

export default defineAppConfig({
  ui: {
    colors: {
      primary: "lime", // 'lime' lebih dekat ke logo Anda daripada 'green'
      neutral: "slate", // 'slate' sudah bagus, atau ganti 'stone' jika ingin lebih hangat
    },
  },
});
